# This file only contains the high-level dependencies without a pinned version.
# Regenerate requirements.txt with the command recorded in its header:
#   uv pip compile requirements.in --python-version=3.13 --output-file=requirements.txt

-e .
